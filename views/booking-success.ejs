<%- include('layout', { title: '訂位成功 - 餐廳訂位系統', body: `
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header bg-success text-white">
                    <h3 class="mb-0">
                        <i class="fas fa-check-circle me-2"></i>訂位成功！
                    </h3>
                </div>
                <div class="card-body">
                    <div class="alert alert-success" role="alert">
                        恭喜您！您的訂位已成功確認。
                    </div>
                    
                    <h5 class="mb-3">訂位詳情</h5>
                    <div class="row">
                        <div class="col-md-6">
                            <p><strong>訂位編號：</strong> ${booking.id}</p>
                            <p><strong>姓名：</strong> ${booking.customer_name}</p>
                            <p><strong>聯絡電話：</strong> ${booking.customer_phone}</p>
                            ${booking.customer_email ? `<p><strong>電子郵件：</strong> ${booking.customer_email}</p>` : ''}
                        </div>
                        <div class="col-md-6">
                            <p><strong>訂位日期：</strong> ${booking.booking_date}</p>
                            <p><strong>訂位時間：</strong> ${booking.booking_time}</p>
                            <p><strong>用餐人數：</strong> ${booking.party_size}人</p>
                            <p><strong>桌位號碼：</strong> ${table.table_number}</p>
                        </div>
                    </div>
                    
                    ${booking.special_requests ? `
                        <div class="mt-3">
                            <h6>特殊需求：</h6>
                            <p class="text-muted">${booking.special_requests}</p>
                        </div>
                    ` : ''}
                    
                    <div class="alert alert-info mt-4" role="alert">
                        <i class="fas fa-info-circle me-2"></i>
                        <strong>重要提醒：</strong>
                        <ul class="mb-0 mt-2">
                            <li>請準時到達，如有延遲請提前致電餐廳</li>
                            <li>如需取消或修改訂位，請提前24小時通知</li>
                            <li>請保留此訂位編號以便查詢</li>
                        </ul>
                    </div>
                    
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                        <a href="/" class="btn btn-outline-primary">
                            <i class="fas fa-home me-2"></i>回到首頁
                        </a>
                        <a href="/booking" class="btn btn-primary">
                            <i class="fas fa-plus me-2"></i>再次訂位
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
` }) %>